---
context: research
phase: 03-configured-session-survivability-dispatch
task: null
total_tasks: null
status: blocked
last_updated: 2026-09-17T05:02:31-04:00
---

# PR #4035 — Claude Implementation Handoff

## Start here

This is a **blocked remediation**, not permission to change code now. PR #4035 is open and changes-requested at `c7855a4bc760f45ea699a9c2d0ee9ecc1e8b5cf5`; the maintainer's review submitted 2026-09-16 reports a merge conflict and three unresolved contracts. Required GitHub checks currently pass, but that only covers the checks configured for this head. It does **not** establish mergeability, correct Markdown dispatch behavior, or a safe foreground path.

Use a clean, isolated worktree. The current checkout has unrelated modified and untracked files; do not reset, clean, stage, or fold them into the PR.

## Blocking constraints — acknowledge before editing

- [ ] **Scope:** #3159 is executor-oriented, but the current PR and ADR thread verifier dispatch. Get maintainer confirmation of **executor-only** remediation, or explicit re-approval for verifier scope.
- [ ] **Config-read policy:** ADR-3159 currently specifies unreadable config → foreground `false`; the author's stated intended correction is preserve default-on `true`. These cannot both be true. Get one written policy, then make ADR, workflow, docs, and tests agree.
- [ ] **Foreground bound:** `session_outlives_turn=false` makes `Agent()` synchronous. The background-polling stall mechanism cannot run while that call blocks, and no enforceable timeout/cancellation primitive is currently established. Obtain one, or make the false path unavailable. Do not add a prose-only timeout claim.
- [ ] **Merge state:** Rebase/merge-resolve only after the three decisions. Verify the checked-out SHA equals the PR head; a previous `gh pr checkout` landed on a stale force-pushed tip.

## What the review found

1. The foreground path can hang indefinitely because the relevant stall detection needs a live orchestrator between background polls.
2. `execute-phase.md` and `session-survivability-dispatch.md` contain independently maintained executor `Agent()` templates. Tests pin both but do not prove parity; this is a two-sources-of-truth defect.
3. Verifier dispatch is included even though the issue/reproduction is executor-focused; scope is not settled.
4. The ADR and the intended config-read behavior conflict.
5. The PR was deliberately paused before the promised fixes landed, and now conflicts with `next`.

Earlier assessment also found that text-presence tests allowed the config-error direction defect to survive. Test behavior at the **actual bounded dispatch block** and generated runtime outputs; do not accept adjacent prose or a single regex as evidence.

## Required reading — in order

1. This file — state, gates, and scope boundaries.
2. `.planning/HANDOFF.json` — machine-readable counterpart and measured dirty-tree list.
3. [Maintainer review on PR #4035](https://github.com/open-gsd/gsd-core/pull/4035) — refresh immediately before planning; it is time-sensitive.
4. `docs/adr/3159-executor-session-survivability-dispatch.md` — the current contradictory contract.
5. `gsd-core/workflows/execute-phase.md` — actual executor and verifier dispatch paths.
6. `gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md` — duplicate template surface.
7. `src/config.cts` — schema and configuration parsing boundary.
8. `tests/execute-phase-active-flags.test.cjs` and `tests/runtime-converters.test.cjs` — current proxy-test surfaces.
9. `.planning/phases/03-configured-session-survivability-dispatch/03-01-PLAN.md` and `03-02-PLAN.md` — prior implementation intent; they are background, not authority over maintainer decisions.

## Recommended implementation sequence

### 0. Close the design gap first

Post or obtain one maintainer response that settles the three checked blockers. Recommended outcome, subject to that response:

- executor-only scope;
- config error produces an explicit, documented outcome rather than silently selecting a mode; and
- no foreground opt-out on a host without a real enforcement bound.

If the maintainer instead chooses default-on or foreground fallback, record the rationale and update every contract surface together. Do not silently choose based on a previous review comment.

### 1. Shrink PR #4035 to the approved fix

- Remove verifier-dispatch changes unless scope is explicitly re-approved.
- Eliminate the second executable executor template. One canonical template must retain the SUMMARY-commit, STATE/ROADMAP, execution-context, and AGENT_SKILLS safeguards.
- Restore unrelated `execute-phase.md` content removed for byte-budget headroom. Do not trade load-bearing workflow behavior for a frozen ceiling without an explicit approval.
- Remove unsupported `.git/config.lock` race claims.
- Resolve the merge conflict after the contract decisions, then reread the final diff from scratch.

Keep broader automation out of this PR unless the maintainer asks for it. The goal is a reviewable repair, not a new framework.

### 2. Implement the agreed configuration behavior

At the parse/config boundary, distinguish missing config from explicit `false` and from unreadable/malformed config. The test matrix must cover all three. Do not make an unreadable value look like an affirmative opt-out.

If foreground mode remains allowed, prove the host has an enforceable bound. If it does not, reject or suppress the false path rather than leaving an unbounded synchronous call.

### 3. Add regression coverage that can fail

- Test `missing`, explicit `true`, explicit `false`, malformed JSON, and unavailable config through the real config/query seam.
- Test each actual executor dispatch mode: harness worktree, orchestrator worktree/process, and no-isolation sequential. Include verifier only if its scope is approved.
- Validate runtime-converted/installed artifacts, not just canonical Markdown.
- Add known-bad mutation fixtures: remove `run_in_background`, invert the config error policy, move the parameter into adjacent prose, remove a required safeguard, and add a shortened duplicate template. Each must produce a specific failing assertion.
- Add property coverage for the boolean parsing boundary if the maintainer keeps this feature; property tests supplement, but do not replace, dispatch behavior tests.
- Add a host-level integration/reproducible harness for the foreground-time-bound contract. Static Markdown inspection cannot prove a one-shot host will not hang.

### 4. Verify the final head, not a historical branch run

1. Build generated runtime artifacts in the clean worktree before direct GSD commands: `npm run build:lib`.
2. Regenerate affected artifacts: `npm run regen:derived`.
3. Run focused tests first, then `npm run lint:ci` and `npm test`.
4. Run the PR worktree's `scratch/ci-mac.sh` preflight as required by this project.
5. Push, then run `gh pr checks 4035 --required` against the new PR head.
6. Re-check PR head, mergeability, maintainer review, generated/install-tree drift, and the final diff. Green checks exercise their configured cases only; they do not substitute for these direct checks.

## Follow-up after the narrow fix

Propose a separate guardrail change only after #4035 is resolved:

- a maintained dispatch-contract/parity check for canonical workflow blocks;
- a PR scope ledger that cross-checks body, ADR, docs, changeset, and changed dispatch routes;
- a final-head preflight that records base/head SHA and refuses to certify a changed head.

## Evidence limits

- Memtrace MCP was unavailable to this session. The maintainer reported a PR graph/index branch mismatch, so zero graph-review findings are **inconclusive**, not a clean bill of health.
- Current required checks passing proves only those jobs passed for the queried PR head. It does not prove the foreground path is bounded or the templates agree.
- The GSD runtime query failed locally because `gsd-core/bin/lib/quick-batch-command-router.cjs` is missing. This handoff is manual; do not rely on `gsd-tools` until a clean-worktree runtime build succeeds.

## Infrastructure state

- No background processes or async jobs were found.
- Current branch when this was written: `personal/workspace`.
- Current checkout is dirty; see `.planning/HANDOFF.json` for the exact measured list.

## Resume command

Start by refreshing PR #4035 and obtaining the three maintainer answers. Only then create an isolated worktree and begin the narrow remediation.
