---
phase: 03-configured-session-survivability-dispatch
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docs/CONFIGURATION.md
  - gsd-core/references/planning-config.md
  - docs/adr/3159-executor-session-survivability-dispatch.md
  - docs/adr/README.md
  - .changeset/<generated-name>.md
  - tests/runtime-converters.test.cjs
  - tests/fixtures/install-tree/*.json
autonomous: false
requirements:
  - SESSION-03
  - QUALITY-03
  - COMPAT-01

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-01, contributors can discover that workflow.session_outlives_turn defaults to true and false is an executor-only opt-out for a parent session that ends with its turn."
    - "The ADR distinguishes agent-tool availability from parent-session survivability and records the executor-only boundary in Proposed status until the decision is ratified."
    - "Runtime conversion and generated install projections retain observable literal true and false executor dispatch branches from the canonical workflow for harness Agent and orchestrator-worktree process-spawn backends."
    - "The projected safe branch remains configuration-selected rather than selected by runtime name, and conversion tests include an opposite control against that shortcut."
    - "ADR index, generated-sync, install projection, and release changeset are synchronized from authored sources rather than hand-edited output."
    - "Focused tests distinguish source/workflow instruction coverage from a live one-shot-host session-lifecycle guarantee."
    - "No test or documentation asserts that verifier dispatch, isolation/worktrees, Shape 2, or SUMMARY recovery was changed."
  artifacts:
    - path: "docs/adr/3159-executor-session-survivability-dispatch.md"
      provides: "Issue-scoped Proposed ADR explaining the host-session assumption and executor-only opt-out"
      contains: "workflow.session_outlives_turn"
    - path: "docs/CONFIGURATION.md"
      provides: "Public configuration reference row with true default and scope fence"
      contains: "workflow.session_outlives_turn"
    - path: "gsd-core/references/planning-config.md"
      provides: "Shipped complete configuration-reference entry"
      contains: "workflow.session_outlives_turn"
    - path: "docs/adr/README.md"
      provides: "Generated index entry for ADR-3159"
      contains: "ADR-3159"
    - path: "tests/runtime-converters.test.cjs"
      provides: "Canonical-to-runtime projection assertions for literal session-survivability branches"
      contains: "session_outlives_turn"
    - path: ".changeset/<generated-name>.md"
      provides: "Post-PR generator-created, actual-PR-number-linked Added release note for the public configuration feature"
      contains: "Added"
  key_links:
    - from: "docs/CONFIGURATION.md and gsd-core/references/planning-config.md"
      to: "workflow.session_outlives_turn"
      via: "matching operator-facing and shipped-reference documentation of default, false mode, and exclusions"
      pattern: "workflow\\.session_outlives_turn"
    - from: "docs/adr/3159-executor-session-survivability-dispatch.md"
      to: "docs/adr/README.md"
      via: "scripts/gen-adr-index.cjs --write creates the index entry instead of a hand-authored table row"
      pattern: "ADR-3159"
    - from: "gsd-core/workflows/execute-phase.md, session-survivability-dispatch.md, and executor-isolation-dispatch.md"
      to: "tests/runtime-converters.test.cjs and tests/fixtures/install-tree/*.json"
      via: "runtime conversion/install generation projects canonical harness-Agent and orchestrator-worktree literal branches to supported runtime artifacts"
      pattern: "run_in_background"
  assumptions:
    - statement: "Spec-less edge probe 1: documentation must describe absence/default true rather than only the false escape hatch."
      status: covered_by_task_1
      flagged: true
    - statement: "Spec-less edge probe 2: documentation must state explicit true remains the normal background path."
      status: covered_by_task_1
      flagged: true
    - statement: "Spec-less edge probe 3: the ADR must describe awaited executor foreground dispatch without claiming inline execution."
      status: covered_by_task_1
      flagged: true
    - statement: "Spec-less edge probe 4: projection tests must retain a literal false branch rather than merely a foreground prose label."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 5: projection tests must retain the literal default true branch as an opposite result."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 6: conversion coverage must reject a runtime-name implementation shortcut."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 7: generated install-tree output must contain the new fragment where applicable and be regenerated, never manually patched."
      status: covered_by_task_2
      flagged: true
  prohibitions:
    - "Do not hand-edit ADR index rows, emitted CJS, install-tree fixture JSON, or a fixed-name changeset; do not claim external host-lifecycle reliability from source/projection tests."
---

<objective>
Complete #3159's release-facing contract and prove that runtime projection retains the intentionally literal executor dispatch behavior.

Purpose: Make the safe opt-out understandable to integrators and prevent canonical workflow changes from drifting across generated ADR and installed-runtime surfaces.
Output: An issue-scoped ADR, synchronized configuration documentation and release note, generated ADR/install outputs, and projection checks with direction and anti-runtime-name controls.
</objective>

<execution_context>
@/home/denniyahh/.codex/gsd-core/workflows/execute-plan.md
@/home/denniyahh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-configured-session-survivability-dispatch/03-CONTEXT.md
@.planning/phases/03-configured-session-survivability-dispatch/03-RESEARCH.md
@.planning/phases/03-configured-session-survivability-dispatch/03-PATTERNS.md
@.planning/phases/03-configured-session-survivability-dispatch/03-VALIDATION.md
@.planning/phases/03-configured-session-survivability-dispatch/MEMORY-RECALL.md
@.planning/phases/03-configured-session-survivability-dispatch/03-01-SUMMARY.md
@CONTRIBUTING.md
@docs/adr/README.md
@docs/adr/1143-claude-orchestration-capability.md
@docs/CONFIGURATION.md
@gsd-core/references/planning-config.md
@gsd-core/workflows/execute-phase.md
@gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md
@tests/runtime-converters.test.cjs
@tests/fixtures/install-tree/claude.json
</context>

## Artifacts this phase produces

- `docs/adr/3159-executor-session-survivability-dispatch.md`, a `Proposed` issue-prefixed ADR, and the generator-owned ADR-index update.
- Matching public and shipped-reference documentation for the default-on executor-only configuration key.
- After projection verification and PR creation, a generator-created actual-PR-number-linked `Added` changeset, canonical-to-runtime converter regressions for both executor backends, and regenerated applicable install-tree fixture output.
- Evidence that generator/build/install projections stay synchronized; this evidence does not substitute for a real host integration test that kills a parent one-shot turn.

## Spec-less fallback record

- No phase SPEC or UI-SPEC exists. This plan carries all seven edge-probe candidates as flagged assumptions, covering documentation of absent/true/false behavior and conversion/install proof of literal, configuration-selected branches.
- Candidate accounting: 7 candidates = 7 task-owned coverage entries; 0 candidates are silently dropped, and no additional prohibition is inferred beyond the approved scope fences.

<tasks>

<task type="auto">
  <name>Task 1: Publish the narrow host-session contract and release metadata</name>
  <reversibility rating="costly">D-01 makes workflow.session_outlives_turn a documented public configuration contract; correcting published wording later requires coordinated documentation, compatibility, and release-note updates.</reversibility>
  <files>docs/CONFIGURATION.md, gsd-core/references/planning-config.md, docs/adr/3159-executor-session-survivability-dispatch.md, docs/adr/README.md</files>
  <read_first>
    - .planning/phases/03-configured-session-survivability-dispatch/03-CONTEXT.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-RESEARCH.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-PATTERNS.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-VALIDATION.md
    - CONTRIBUTING.md
    - docs/adr/README.md
    - docs/adr/1143-claude-orchestration-capability.md
    - docs/CONFIGURATION.md
    - gsd-core/references/planning-config.md
  </read_first>
  <action>
    Per D-01 through D-03, add a `workflow.session_outlives_turn` row beside the closest workflow escape-hatch entries in both configuration references. State boolean type and default `true`; explain that `false` keeps `gsd-executor` agents but requests foreground, awaited executor dispatch when the parent session does not survive the turn. State the boundaries in the documentation itself: this setting does not infer tool availability, select a runtime, alter isolation/worktree behavior, or alter verifier dispatch.

    Add `docs/adr/3159-executor-session-survivability-dispatch.md` using the issue-prefix naming and metadata/status grammar from the ADR README. Use `Proposed` status while the decision awaits post-shipment ratification. Record the problem distinction between agent-tool availability and parent-session survivability, the default true/explicit false decision, literal foreground/background requirement across the supported executor backends, executor-only scope, alternatives excluded by the maintainer-approved feature, and consequences/verification limits. Use valid issue links and only resolvable relative links. Run `node scripts/gen-adr-index.cjs --write` to update the generated section of `docs/adr/README.md`; never compose an index row by hand. Do not create, claim completion of, or lint the PR-numbered changeset in this task: task 03-02-03 owns it only after all implementation/projection verification and actual PR creation.
  </action>
  <acceptance_criteria>
    - Both configuration references name the exact key, boolean type, default true, false-mode executor-only behavior, and non-goals for runtime selection, tool availability, isolation/worktrees, and verifier dispatch.
    - ADR-3159 is issue-prefixed, marked Proposed, gives a valid #3159 issue link, and distinguishes session survival from agent-tool availability without overstating host reliability.
    - The ADR records D-01, D-02, and D-03 exactly and its scope does not expand into Shape 2 or recovery work.
    - The ADR index is generator-produced and passes validation; no generated index row was written manually.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/gen-adr-index.cjs --check &amp;&amp; npm run lint:generated-sync &amp;&amp; git diff --check</automated>
  </verify>
  <done>Integrators can find an accurately scoped executor-session opt-out, and the ADR/release surfaces are generated and validated from their canonical sources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove runtime conversion and installed-artifact preservation of both dispatch directions</name>
  <files>tests/runtime-converters.test.cjs, tests/fixtures/install-tree/*.json</files>
  <read_first>
    - .planning/phases/03-configured-session-survivability-dispatch/03-RESEARCH.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-PATTERNS.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-VALIDATION.md
    - gsd-core/workflows/execute-phase.md
    - gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md
    - gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md
    - src/runtime-artifact-conversion.cts
    - tests/runtime-converters.test.cjs
    - tests/fixtures/install-tree/claude.json
    - scripts/gen-adr-index.cjs
  </read_first>
  <behavior>
    - D-01/D-02 PROJECTION / HARNESS BACKEND: conversion retains a configuration-gated literal `run_in_background: true` harness Agent path and a distinct literal `run_in_background: false` harness path.
    - D-01/D-02 PROJECTION / ORCHESTRATOR-WORKTREE BACKEND: conversion retains background process spawning for default/true and synchronous foreground process spawning plus completion wait for false, using the already-resolved mode rather than another config read.
    - NEGATIVE CONTROL: neither backend's false region has a background instruction, neither true region has a foreground/synchronous instruction, and a test cannot pass by searching global workflow text that includes unrelated Agent calls.
    - D-03 SCOPE CONTROL: conversion assertions reject a `$RUNTIME`/runtime-name condition as the selector and retain verifier behavior plus isolation/worktree ownership while allowing only the existing isolation fragment's dispatch mode to vary.
    - INSTALL CONTROL: regenerated applicable install-tree output contains the new workflow fragment and updated isolation fragment under the projected execute-phase step path and derived-sync/install tests remain green.
  </behavior>
  <action>
    First add focused failing assertions to `tests/runtime-converters.test.cjs` using the closest existing typed-condition and literal-flag converter tests. Convert the canonical workflow and both executor fragments through the existing supported runtime seam. Extract the harness Agent and orchestrator-worktree process-spawn regions separately rather than matching every `Agent(` block in all source text: assert the harness literal branches, the orchestrator-worktree true-background/false-synchronous-and-waiting branches, and opposite-direction controls for each backend. Include a negative assertion that the session-survivability decision is not guarded by runtime identity; test verifier behavior and isolation/worktree ownership separately enough to prove D-03 scope is preserved. These tests prove projection of the authored contract, not external parent-session persistence.

    Regenerate applicable install-tree fixtures using the repository-owned build/generation path after the canonical workflow/fragments exist. Update only generator-produced fixture diffs, including the execute-phase step entries for the new session-survivability fragment and modified isolation fragment where each runtime installs them; do not hand-edit JSON or emitted CJS. Run the focused converter suite, install suite, ADR generated-index check, and generated-sync check. Finish with the validation strategy's full contributor gate before marking implementation/projection verification ready for the ship handoff; if a command fails, preserve its output and resolve the current upstream failure rather than treating historical green branch runs as proof. Do not create or lint the final PR-numbered changeset here because task 03-02-03 must run after the branch is pushed and the actual feature PR exists.
  </action>
  <acceptance_criteria>
    - Runtime-converter tests exercise canonical-to-emitted harness Agent literal true/false branches and orchestrator-worktree true-background/false-synchronous-and-waiting branches, with backend-scoped opposite controls.
    - The test suite rejects a runtime-name selector and does not accidentally satisfy assertions from unrelated verifier/debug Agent calls or the other executor backend.
    - Regenerated install-tree output includes the new session-survivability fragment and modified isolation fragment wherever the canonical execute-phase workflow is installed, with no hand-authored fixture or emitted-runtime edit.
    - `npm run test:install`, `npm run lint:generated-sync`, and ADR-index validation pass before the ship handoff; no final changeset is represented as complete until task 03-02-03 receives the actual PR number.
    - The final verification report names the evidence limit: passing source/converter/install checks does not prove every wrapper host preserves a parent turn; the host integration flow remains the manual SESSION-01 evidence.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:lib &amp;&amp; node --test tests/runtime-converters.test.cjs tests/adr-index-gate.test.cjs &amp;&amp; npm run test:install &amp;&amp; npm run lint:generated-sync &amp;&amp; node scripts/gen-adr-index.cjs --check &amp;&amp; mise run check:budget &amp;&amp; npm run build &amp;&amp; npm run lint:ci &amp;&amp; npm test</automated>
  </verify>
  <done>Generated ADR and install artifacts are synchronized, runtime conversion retains both literal executor directions under the public configuration choice, and the project gates have evidence-backed results with stated limits; the plan pauses before the PR-numbered changeset handoff.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Create the real feature PR, then generate and backfill its release fragment</name>
  <precondition>Task 03-02-02 has recorded green implementation/projection verification and the branch is ready to push without an incomplete release fragment being treated as final.</precondition>
  <files>.changeset/&lt;generated-name&gt;.md</files>
  <action>
    Ship handoff: push the verified feature branch and create the feature PR against `next`, then obtain that PR's actual number. Do not assume GitHub issue #3159 is the PR number. Only after the real PR exists, generate the release fragment with the repository-supported command `node scripts/changeset/new.cjs --type Added --pr &lt;actual-pr-number&gt; --body "Add the executor session-survivability dispatch opt-out."`; commit and push the generator-created `.changeset/&lt;generated-name&gt;.md`, then run `GITHUB_BASE_REF=next node scripts/changeset/lint.cjs`. If an interim placeholder is required solely to open the PR, mark it incomplete and not for final push, then immediately regenerate/backfill the fragment with the actual PR number before any final push or readiness claim. Do not hand-select its filename, edit generated JSON/CJS, or continue if the real PR number is unavailable.
  </action>
  <instructions>
    The implementation, projection, install, generated-sync, and broad repository checks from Task 03-02-02 are complete before this handoff.
    Push the verified branch and create the feature PR against `next`, or, if that action is blocked by authentication or required maintainer coordination, provide the resulting actual PR number after completing that external step.
  </instructions>
  <verification>Confirm the PR targets `next`, inspect the generated fragment for that exact PR number, then run `GITHUB_BASE_REF=next node scripts/changeset/lint.cjs` after commit/push.</verification>
  <resume-signal>Provide the real feature PR number, or confirm that the pushed PR exists and is ready for the executor to generate/backfill the changeset.</resume-signal>
  <acceptance_criteria>
    - A real feature PR targeting `next` exists and its actual PR number, not issue #3159, is recorded in the generated release fragment.
    - The fragment is created or regenerated through `scripts/changeset/new.cjs` with `--type Added`, the real `--pr` number, and a concrete `--body`; no unsupported generator option or fixed planned filename is used.
    - The generated fragment is committed and pushed after the actual PR number is available, and `GITHUB_BASE_REF=next node scripts/changeset/lint.cjs` passes only after that backfill.
    - A placeholder, if used, is never reported as completed release metadata and is replaced before final push/readiness.
  </acceptance_criteria>
  <verify>
    <automated>GITHUB_BASE_REF=next node scripts/changeset/lint.cjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The verified feature PR has an actual-number-linked Added changeset generated with the supported command, committed/pushed after PR creation, and linted successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical workflow/fragments → runtime converter | Shipped Markdown syntax for harness Agent and orchestrator-worktree process spawning is transformed into runtime-specific instruction surfaces. |
| Authored ADR → generated ADR index | Release-facing documentation becomes generated index content and must retain valid status/link relationships. |
| Generator output → installed runtime tree | Generated fixture/install assets are evidence of shipped projection and must not diverge from the authored workflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | both executor backend conversion assertions | high | mitigate | Assert harness literal true/false and orchestrator-worktree background/synchronous-wait regions after conversion with backend-scoped negative controls, rather than a global source token count. |
| T-03-05 | Denial of service | default dispatch documentation/projection | high | mitigate | Document default true and preserve/test the projected true branch so the opt-out cannot silently become universal foreground dispatch. |
| T-03-06 | Tampering | ADR/index/install generated artifacts | high | mitigate | Use repository generators, generated-sync, ADR-index checks, and install tests; forbid manual edits of owned output. |
| T-03-07 | Tampering | runtime selector and isolation handoff policy | high | mitigate | Explicitly reject runtime-name dispatch gating and prove the already-resolved configuration flows through the existing isolation fragment without ownership changes. |
| T-03-SC | Tampering | npm/pip/cargo supply chain | high | mitigate | No package install is allowed. If an unexpected dependency becomes necessary, stop for the package-legitimacy gate; no lockfile or manifest change is in scope. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Host integrator discoverability and default-preserving behavior across distributed runtime artifacts | 03-02 | COVERED | Documentation task, both-backend converter/install generation task, then actual-PR release handoff. |
| REQ | SESSION-03 | ADR and configuration reference explain host-session assumption and opt-out | 03-02 | COVERED | Task 1 writes both references and ADR-3159. |
| REQ | QUALITY-03 | Projection tests include opposite-direction controls | 03-02 | COVERED | Task 03-02-02 adds harness and orchestrator-worktree branch plus runtime-name negative controls. |
| REQ | COMPAT-01 | Generated runtime artifacts remain synchronized | 03-02 | COVERED | Task 03-02-02 regenerates applicable install output and runs install/generated gates. |
| RESEARCH | — | Proposed ADR, generated index, changeset, and complete-reference documentation | 03-02 | COVERED | Task 03-02-01 owns ADR/docs; Task 03-02-03 generates the real-PR changeset only after verification. |
| RESEARCH | — | Converter/install proof for canonical literal branches | 03-02 | COVERED | Task 03-02-02 uses existing converter and fixture owners, never hand edits outputs. |
| CONTEXT | D-01 | Public default-on configuration contract | 03-02 | COVERED | Task 1 documents exact default/name; Task 2 proves projected literal branches. |
| CONTEXT | D-02 | Executor agents and awaited false behavior only | 03-02 | COVERED | ADR/docs scope and converter assertions retain executor-local choice. |
| CONTEXT | D-03 | Verifier dispatch unchanged | 03-02 | COVERED | Documentation boundaries and branch-scoped conversion tests exclude verifier changes. |

<verification>
- After Task 1: `node scripts/gen-adr-index.cjs --check && npm run lint:generated-sync && git diff --check` proves ADR/documentation index and derived-sync state. It does not prove runtime conversion or host lifecycle behavior.
- After Task 2: `npm run build:lib && node --test tests/runtime-converters.test.cjs tests/adr-index-gate.test.cjs && npm run test:install && npm run lint:generated-sync && node scripts/gen-adr-index.cjs --check` proves exercised conversion/install/index paths. It does not prove every external wrapper preserves the parent turn.
- After Task 3: `GITHUB_BASE_REF=next node scripts/changeset/lint.cjs && git diff --check` proves the generator-created changeset carries the actual PR number and has lint/whitespace health. It does not prove PR review readiness or merge status.
- Before phase verification: `mise run check:budget && npm run build && npm run lint:ci && npm test` exercises the broad contributor gate before the ship handoff. Green results prove those checked repository cases, not network-independent reliability or a real one-shot host integration; record any unavailable command/output rather than claiming a pass.
</verification>

<success_criteria>
- The public docs, complete config reference, ADR, generated ADR index, and post-PR actual-number changeset consistently describe the approved executor-only contract.
- Converted and installed workflow artifacts retain branch-distinct literal true/false executor instructions and reject a runtime-name shortcut.
- Generated artifacts and repository verification gates are run through their owning commands, with their limits reported accurately.
</success_criteria>

<output>
Create `.planning/phases/03-configured-session-survivability-dispatch/03-02-SUMMARY.md` when done.
</output>
