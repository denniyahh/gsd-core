---
phase: 03-configured-session-survivability-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-core/bin/shared/config-schema.manifest.json
  - gsd-core/bin/shared/config-defaults.manifest.json
  - src/config.cts
  - gsd-core/workflows/execute-phase.md
  - gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md
  - gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md
  - tests/config.test.cjs
  - tests/config-get-default.test.cjs
  - tests/execute-phase-active-flags.test.cjs
  - .planning/phases/03-configured-session-survivability-dispatch/03-VALIDATION.md
autonomous: true
requirements:
  - SESSION-01
  - SESSION-02
  - QUALITY-03

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Per D-01, an absent workflow.session_outlives_turn resolves to true, explicit true resolves to true, and explicit false remains observably distinct."
    - "Per D-02 and SESSION-01, false reaches both executor backends: harness Agent dispatches and orchestrator-worktree process-spawn dispatches are explicit foreground/synchronous operations awaited before the next executor."
    - "Per D-01 and D-02, absent/true retains the existing literal background dispatch for both harness Agent and orchestrator-worktree process-spawn executor backends, never selecting a false-mode instruction."
    - "A non-boolean config-set value is rejected at the configuration boundary and cannot quietly select either lifecycle mode."
    - "The false executor region for each backend contains no literal background instruction, and the default/true region for each backend contains no literal foreground/synchronous instruction."
    - "Per D-03, verifier dispatch remains unchanged; existing isolation/worktree ownership remains unchanged while executor-isolation dispatch consumes the already-resolved session-survivability mode without re-resolving configuration."
    - "The canonical workflow exposes literal true and false branches suitable for later runtime-conversion and installed-projection checks."
  artifacts:
    - path: "gsd-core/bin/shared/config-schema.manifest.json"
      provides: "Central public-key registration for workflow.session_outlives_turn"
      contains: "workflow.session_outlives_turn"
    - path: "gsd-core/bin/shared/config-defaults.manifest.json"
      provides: "Nested workflow default of true for newly materialized project configuration"
      contains: "session_outlives_turn"
    - path: "src/config.cts"
      provides: "Schema-default resolution and boolean config-set validation for workflow.session_outlives_turn"
      contains: "workflow.session_outlives_turn"
    - path: "gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md"
      provides: "Harness Agent executor literal background and awaited foreground branches"
      contains: "run_in_background"
    - path: "gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md"
      provides: "Orchestrator-worktree process-spawn dispatch that preserves ownership while honoring the already-resolved session-survivability mode"
      contains: "SESSION_OUTLIVES_TURN"
    - path: "tests/execute-phase-active-flags.test.cjs"
      provides: "Workflow-product direction and executor-only scope controls"
      contains: "session_outlives_turn"
  key_links:
    - from: "gsd-core/bin/shared/config-schema.manifest.json"
      to: "src/config.cts::isValidConfigKey and cmdConfigSet"
      via: "central manifest registration accepts the public key instead of a workflow-local parser"
      pattern: "workflow\\.session_outlives_turn"
    - from: "gsd-core/bin/shared/config-defaults.manifest.json and src/config.cts::SCHEMA_DEFAULTS"
      to: "src/config.cts::cmdConfigGet"
      via: "new-project and old/minimal project reads both resolve the documented true default"
      pattern: "session_outlives_turn"
    - from: "gsd-core/workflows/execute-phase.md"
      to: "gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md and executor-isolation-dispatch.md"
      via: "the canonical workflow resolves the key once; harness and orchestrator-worktree executor backends consume that resolved mode without changing isolation ownership"
      pattern: "session-survivability-dispatch"
    - from: "tests/execute-phase-active-flags.test.cjs"
      to: "harness Agent blocks and orchestrator-worktree process-spawn blocks in canonical executor fragments"
      via: "exhaustive backend-scoped assertions inspect literal dispatch arguments, synchronous waiting, and verifier scope"
      pattern: "run_in_background"
  assumptions:
    - statement: "Spec-less edge probe 1: absent configuration must be observed as true through config-get rather than inferred from a shell fallback."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 2: explicit true must remain observably equivalent to absent configuration and different from explicit false."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 3: explicit false must select an awaited literal foreground executor call."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 4: malformed non-boolean input must fail without becoming a plausible lifecycle value."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 5: the false branch must be free of executor background instructions."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 6: verifier dispatch must remain outside the new key's decision region."
      status: covered_by_task_2
      flagged: true
    - statement: "Spec-less edge probe 7: literal branches must survive canonical-to-runtime projection; Plan 03-02 owns the emitted-artifact proof."
      status: covered_by_plan_03_02
      flagged: true
  prohibitions:
    - "Do not select behavior by runtime name, alter dispatch isolation/worktree ownership, change verifier dispatch, implement Shape 2, or add orchestrator-owned SUMMARY recovery."
---

<objective>
Establish the upstream-current configuration and executor-dispatch contract for #3159 before release-surface work expands it.

Purpose: Give one-shot hosts an explicit safe executor path without changing the established asynchronous default for every other integrator.
Output: A current-upstream feature baseline, a public default-on boolean configuration contract, a small executor-only dispatch fragment, and behavioral tests that prove both literal directions.
</objective>

<execution_context>
@/home/denniyahh/.codex/gsd-core/workflows/execute-plan.md
@/home/denniyahh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-configured-session-survivability-dispatch/03-CONTEXT.md
@.planning/phases/03-configured-session-survivability-dispatch/03-RESEARCH.md
@.planning/phases/03-configured-session-survivability-dispatch/03-PATTERNS.md
@.planning/phases/03-configured-session-survivability-dispatch/03-VALIDATION.md
@.planning/phases/03-configured-session-survivability-dispatch/MEMORY-RECALL.md
@CONTRIBUTING.md
@gsd-core/bin/shared/config-schema.manifest.json
@gsd-core/bin/shared/config-defaults.manifest.json
@src/config.cts
@gsd-core/workflows/execute-phase.md
@gsd-core/workflows/execute-phase/steps/per-plan-executor-routing.md
@gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md
@tests/config.test.cjs
@tests/config-get-default.test.cjs
@tests/execute-phase-active-flags.test.cjs
</context>

## Artifacts this phase produces

- The public `workflow.session_outlives_turn` boolean, registered centrally with a default of `true` for both newly initialized and pre-existing/minimal configurations.
- A small canonical workflow fragment with visible harness-Agent literal branches, plus the existing isolation fragment carrying the already-resolved mode into orchestrator-worktree process spawning: default retains background dispatch and false is synchronous/awaited for both backends.
- Owning-suite configuration and backend-scoped workflow-product regressions that prove value direction, malformed-input rejection, branch exclusivity, executor-only scope, and both executor backends.
- No runtime-name selector, verifier-dispatch change, isolation/worktree change, Shape 2 redesign, `SUMMARY.md` recovery mechanism, package dependency, or hand-edited emitted artifact.

## Spec-less fallback record

- No phase SPEC or UI-SPEC exists. Seven edge-probe candidates are explicitly preserved as the seven flagged `must_haves.assumptions` above: absent, true, false, malformed input, false-branch exclusivity, verifier scope, and emitted projection.
- Candidate accounting: 7 candidates = 6 covered by Task 03-01-02 + 1 covered by Task 03-02-02; 0 candidates are silently dropped and 0 new fallback prohibitions are invented.
- Existing source artifacts provide the actual scope fences. Their executor-only, configuration-not-runtime, and no-recovery decisions are repeated in task actions and acceptance criteria rather than inferred as broader feature work.

<tasks>

<task type="auto">
  <name>Task 1: Rebase onto current upstream and align the five-task validation map</name>
  <precondition>The working tree is either clean or every pre-existing user change has been identified and preserved before branch operations begin.</precondition>
  <files>.planning/phases/03-configured-session-survivability-dispatch/03-VALIDATION.md</files>
  <action>
    Before changing any authored source, fetch `upstream`, record the exact current `upstream/next` SHA, and create or rebase the feature branch from that commit. The planning worktree is known stale, so do not edit its inherited production snapshot and do not treat the research SHA as a permanent baseline. Use read-only status/diff checks first; preserve any concurrent user edits and halt rather than resetting or overwriting them. Inspect the current versions of the configuration manifests, `src/config.cts`, canonical execute-phase workflow, `session-survivability-dispatch.md`, `executor-isolation-dispatch.md`, and owner tests after the rebase so task 03-01-02 adapts to upstream movement. Confirm Node/npm engines and install state using repository-native checks; do not invoke DevFlow, add packages, alter lockfiles, or begin source edits if the upstream baseline cannot be established.

    Before source edits, replace the provisional per-task map in `03-VALIDATION.md` with five unambiguous rows matching this plan set: 03-01-01 is the upstream-baseline/preflight command `git fetch upstream && git merge-base --is-ancestor upstream/next HEAD && git diff --check && npm run check:env`; 03-01-02 covers SESSION-01, SESSION-02, and QUALITY-03 with `npm run build:lib && node --test tests/config.test.cjs tests/config-get-default.test.cjs tests/execute-phase-active-flags.test.cjs`; 03-02-01 covers SESSION-03 with `node scripts/gen-adr-index.cjs --check && npm run lint:generated-sync`; 03-02-02 covers QUALITY-03 and COMPAT-01 with `npm run build:lib && node --test tests/runtime-converters.test.cjs tests/adr-index-gate.test.cjs && npm run test:install && npm run lint:generated-sync`; and 03-02-03 is the post-verification ship handoff, with no requirement marked complete until a pushed branch has a real PR number and `GITHUB_BASE_REF=next node scripts/changeset/lint.cjs` passes after generated changeset backfill. Keep the existing manual wrapper-host limitation and update no other planning artifact.
  </action>
  <acceptance_criteria>
    - The executor records a concrete `upstream/next` SHA and performs all later source work from a branch based on that SHA.
    - A read-only status/diff inspection establishes that no unrelated local edit was overwritten; a rebase conflict or unavailable upstream is reported as a blocking fact rather than guessed through.
    - The current upstream versions of every source/test seam named in `<context>` have been inspected once before implementation, including both executor backend fragments and any changed generated-artifact command.
    - `03-VALIDATION.md` has one unambiguous row for each of 03-01-01, 03-01-02, 03-02-01, 03-02-02, and 03-02-03 with the focused command and requirement ownership named above; the existing manual wrapper-host limitation remains present.
    - This task does not modify product files, generated output, dependencies, or runtime configuration; its only file edit is the task-map alignment in `03-VALIDATION.md`.
  </acceptance_criteria>
  <verify>
    <automated>git fetch upstream &amp;&amp; git merge-base --is-ancestor upstream/next HEAD &amp;&amp; git diff --check &amp;&amp; npm run check:env</automated>
  </verify>
  <done>The implementation branch is demonstrably based on current upstream/next, the worktree is understood without destructive cleanup, and the validation map matches all five executable task IDs before source work starts.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Wire the default-preserving executor dispatch tracer end to end</name>
  <reversibility rating="costly">D-01 publishes a configuration key across configuration files, workflow product text, and documentation; renaming or reversing it later requires a coordinated compatibility change, but the operator has already locked the decision.</reversibility>
  <files>gsd-core/bin/shared/config-schema.manifest.json, gsd-core/bin/shared/config-defaults.manifest.json, src/config.cts, gsd-core/workflows/execute-phase.md, gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md, gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md, tests/config.test.cjs, tests/config-get-default.test.cjs, tests/execute-phase-active-flags.test.cjs</files>
  <read_first>
    - .planning/phases/03-configured-session-survivability-dispatch/03-CONTEXT.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-RESEARCH.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-PATTERNS.md
    - .planning/phases/03-configured-session-survivability-dispatch/03-VALIDATION.md
    - gsd-core/bin/shared/config-schema.manifest.json
    - gsd-core/bin/shared/config-defaults.manifest.json
    - src/config.cts
    - src/config-schema.cts
    - gsd-core/workflows/execute-phase.md
    - gsd-core/workflows/execute-phase/steps/per-plan-executor-routing.md
    - gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md
    - tests/config.test.cjs
    - tests/config-get-default.test.cjs
    - tests/execute-phase-active-flags.test.cjs
  </read_first>
  <behavior>
    - RED / D-01: a project with no explicit key returns raw `true`; after setting false it returns raw `false`; after setting true it returns raw `true`; the three observed outputs prove absence is not treated as false.
    - RED / D-01: a non-boolean config-set input fails and leaves no accepted value that reads as either lifecycle boolean.
    - RED / D-02 / SESSION-01 HARNESS BACKEND: the resolved false harness executor branch contains an actual literal `run_in_background: false` Agent call and waits for that result before dispatching another executor.
    - RED / D-02 / SESSION-01 ORCHESTRATOR-WORKTREE BACKEND: the already-resolved false mode reaches the process-spawn dispatch in `executor-isolation-dispatch.md`, runs it synchronously/foreground, and waits for completion before the next executor while retaining worktree ownership.
    - NEGATIVE CONTROL / D-01 and D-02: each backend's default/true region retains its existing literal background dispatch; each false region contains no background instruction and each true region contains no foreground/synchronous instruction.
    - SCOPE CONTROL / D-03: verifier dispatch has no reference to `workflow.session_outlives_turn`; isolation/worktree ownership, recovery, capability, and runtime selection remain unchanged while the isolation fragment receives the already-resolved mode.
  </behavior>
  <action>
    Start with failing owning-suite cases, then make the minimum authored-source changes that turn each case green. Per D-01, add only `workflow.session_outlives_turn` to the central key whitelist, add nested `workflow.session_outlives_turn: true` to the canonical defaults manifest, add the same true schema default in `src/config.cts`, and copy the established boolean validator shape so `config-set` rejects non-boolean parsed values. Exercise all `cmdConfigGet` missing-value paths through the existing schema-default seam; do not make a shell `echo true` the policy source, add a speculative flat `config-loader` default, or add another configuration parser.

    In the canonical execute-phase initialization, resolve this public key once with a default-preserving raw read whose fallback is defensive only. Per D-02, extract the harness-Agent dispatch choice to `gsd-core/workflows/execute-phase/steps/session-survivability-dispatch.md`: retain a literal default/true executor `Agent()` instruction with `run_in_background: true`, and place a separate literal false-mode executor `Agent()` instruction with `run_in_background: false` followed by explicit collection/awaiting before the next executor. Carry that already-resolved mode into `executor-isolation-dispatch.md` without re-reading configuration: for its orchestrator-worktree process-spawn backend, preserve the existing worktree creation/ownership/recovery commands, but select the existing background spawn only for absent/true and a synchronous foreground process-spawn plus completion wait for false before another executor begins. Make both backend choices visible as literal true/false/synchronous instructions, not an omitted flag or a single interpolation placeholder. Do not replace executor work with inline execution, branch on `RUNTIME`, touch verifier dispatch, change `workflow.use_worktrees`, alter isolation selection or worktree ownership, implement Shape 2, or add orchestrator-owned `SUMMARY.md` recovery.

    In the owning configuration suites, prove absent → true, false → false, true → true, and invalid input rejection with indexed/read-back assertions that distinguish missing from boolean values. In the runtime-loaded workflow product suite, inspect backend-scoped regions rather than globally counting a token: exhaustively assert harness Agent false calls are literal false and awaited, harness true calls are literal true, orchestrator-worktree false process spawning is synchronous and waited, and orchestrator-worktree true retains background spawning. Add opposite controls for both backends and assert verifier dispatch has no new key reference. Keep the tests focused on shipped workflow instructions; report that they establish instruction/projection eligibility, not actual one-shot host termination reliability.
  </action>
  <acceptance_criteria>
    - The central schema and canonical defaults accept and materialize exactly `workflow.session_outlives_turn` with default true, and `src/config.cts` gives pre-existing/minimal projects the same effective value.
    - `config-set` accepts JSON booleans true and false, rejects a non-boolean value, and config-get tests index values so absent cannot silently read like false.
    - The canonical workflow resolves the key once, delegates harness invocation to the new fragment, carries the resolved mode into the existing isolation fragment, and contains separate literal true and false harness Agent-call branches; there is no placeholder dispatch argument.
    - Both executor backends honor false before another executor begins: harness calls are explicit `run_in_background: false` and awaited, while orchestrator-worktree process spawning is synchronous/foreground and completion-waited; absent/true preserves each backend's background behavior.
    - Backend-scoped workflow-product tests prove both directions and opposite controls for harness Agent and orchestrator-worktree process-spawn paths; they do not treat prose, a single token occurrence, or an unrelated Agent block as a foreground guarantee.
    - Verifier dispatch, isolation selection/worktree ownership, runtime-name selection, recovery, Shape 2, and dependency/lockfile state remain unchanged by this task.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:lib &amp;&amp; node --test tests/config.test.cjs tests/config-get-default.test.cjs tests/execute-phase-active-flags.test.cjs &amp;&amp; git diff --check</automated>
  </verify>
  <done>The executable workflow and configuration CLI offer a tested, executor-only literal foreground opt-out while absent and true retain the existing literal background behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project configuration → config CLI | Project-controlled values become a public execution-lifecycle choice and must remain typed. |
| Resolved config → workflow dispatch | A boolean selects a literal executor invocation contract in shipped Markdown. |
| Executor backend fragments → host runtime | Runtime conversion interprets harness Agent and orchestrator-worktree process-spawn instructions; neither backend may infer host identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `src/config.cts` config-set/get boundary | high | mitigate | Register the central key, use the established boolean type guard, and test absent/true/false plus a hostile non-boolean value. |
| T-03-02 | Denial of service | harness Agent and orchestrator-worktree executor dispatch | high | mitigate | Keep backend-specific literal background and foreground/synchronous branches separate; test false-mode waiting and both opposite-direction controls so a universal or backend-skipping regression is visible. |
| T-03-03 | Tampering | execute-phase and isolation-fragment policy handoff | high | mitigate | Resolve the integrator configuration once, carry it into process-spawn dispatch, and prohibit runtime-name, isolation-ownership, worktree, verifier, and recovery shortcuts in D-02/D-03 tests. |
| T-03-SC | Tampering | npm/pip/cargo supply chain | high | mitigate | No package installation, manifest, or lockfile change is permitted; halt for a package-legitimacy review if implementation unexpectedly needs a dependency. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Default-preserving opt-out from background executor dispatch for a non-surviving parent turn | 03-01 | COVERED | Task 03-01-02 wires key → config resolution → both backend dispatches and backend-scoped proof. |
| REQ | SESSION-01 | Explicit false selects a foreground, awaited executor path | 03-01 | COVERED | D-02 tracer covers literal harness calls and synchronous orchestrator-worktree process spawning. |
| REQ | SESSION-02 | Absent/enabled configuration retains current background dispatch | 03-01 | COVERED | Schema default plus absent/true CLI and literal-background negative controls. |
| REQ | QUALITY-03 | Behavioral tests exercise both values and opposite controls | 03-01 | COVERED | Task 03-01-02 owns config and both-backend workflow-product matrix. |
| RESEARCH | — | Establish current upstream/next, not stale planning tree, before edits | 03-01 | COVERED | Task 1 is an explicit source-edit gate. |
| RESEARCH | — | Central key/default/type-validation seam and extracted literal workflow fragment | 03-01 | COVERED | Task 2 follows the named existing owners and fragments. |
| CONTEXT | D-01 | Public boolean name; absent/true default and false opt-out | 03-01 | COVERED | Task 2 cites and implements D-01 exactly. |
| CONTEXT | D-02 | Keep executor agents; literal foreground false and awaiting | 03-01 | COVERED | Task 2 cites and implements D-02 exactly. |
| CONTEXT | D-03 | Executor-only boundary | 03-01 | COVERED | Task 2 has verifier and isolation negative controls. |

<verification>
- After Task 1: `git fetch upstream && git merge-base --is-ancestor upstream/next HEAD && git diff --check && npm run check:env` establishes the implementation baseline and whitespace health. It does not establish behavior or rebase-conflict freedom beyond the observed branch state.
- After Task 2: `npm run build:lib && node --test tests/config.test.cjs tests/config-get-default.test.cjs tests/execute-phase-active-flags.test.cjs` proves the exercised configuration and shipped workflow-product branches. It does not establish real parent-turn survival on every external host version or installed runtime projection; Plan 03-02 adds projection evidence.
</verification>

<success_criteria>
- Current upstream/next is the verified source baseline before implementation begins.
- `workflow.session_outlives_turn` is a boolean default-on public configuration with absence, true, false, and malformed-value behavior tested through existing owners.
- The only changed dispatch decision is executor invocation: false is literal foreground and awaited; absent/true remains literal background; verifier/isolation/recovery behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-configured-session-survivability-dispatch/03-01-SUMMARY.md` when done.
</output>
