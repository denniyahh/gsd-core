---
phase: 01-planned-phase-state-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state.cts
  - tests/state.test.cjs
autonomous: true
requirements:
  - STATE-01
  - QUALITY-01
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "As a GSD project maintainer, I want `state planned-phase` to retain authoritative activity metadata when same-date body prose is stale, so that planning cannot erase the recorded activity."
    - "A planned-phase transition whose body activity date does not conflict with the pre-transition frontmatter continues to write the transition's intended activity metadata."
    - "A planned-phase update still applies its intended status, body/current-position, and explicit plan-count changes."
    - "The focused command-level regression reads the written STATE.md artifact and proves both preservation and intended planned-phase effects."
  artifacts:
    - path: src/state.cts
      provides: Lock-safe, same-date activity-conflict use of the existing authoritative-frontmatter reassertion seam.
    - path: tests/state.test.cjs
      provides: Command-level regressions using deterministic same-date conflict and non-conflicting activity fixtures plus strict raw frontmatter parsing.
  key_links:
    - from: cmdStatePlannedPhase in src/state.cts
      to: readModifyWriteStateMd authoritativeFm handling
      via: lock-held pre-transition values only when the body has the same activity date but a conflicting description
      reason: The final synchronized write must retain authoritative metadata for the stale-prose conflict without suppressing ordinary transition-derived activity.
    - from: tests/state.test.cjs
      to: final .planning/STATE.md in its temporary project
      via: runGsdTools state planned-phase followed by parseFrontmatter
      reason: The regression validates the serialized artifact, not a re-derived read model.
---

<objective>
Repair the planned-phase state write so authoritative same-date activity metadata survives the final STATE.md synchronization, with a command-level regression covering the complete behavior.

Purpose: Preserve the already-recorded activity history while retaining the normal planned-phase transition.
Output: A focused state-write repair in `src/state.cts` and focused behavioral coverage in `tests/state.test.cjs`.
</objective>

<execution_context>
@/home/denniyahh/.codex/gsd-core/workflows/execute-plan.md
@/home/denniyahh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@scratch/UPSTREAM-GSD-ISSUES.md
@src/state.cts
@src/state-transition.cts
@tests/state.test.cjs
@tests/helpers.cjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve authoritative planned-phase activity metadata through the locked write path</name>
  <reversibility rating="reversible">The repair is confined to the existing internal authoritative-frontmatter option and can be reverted without a persisted-format migration.</reversibility>
  <files>src/state.cts, tests/state.test.cjs</files>
  <read_first>
    - src/state.cts: `ReadModifyWriteOptions`, `readModifyWriteStateMd`, and `cmdStatePlannedPhase`, especially the existing `authoritativeFm` reassertion flow.
    - src/state-transition.cts: `plannedPhaseCore` and `applyStatePreservation`, to preserve their current body, status, and progress responsibilities.
    - tests/state.test.cjs: existing `state planned-phase` command fixtures and the raw-STATE.md behavioral-test convention.
    - tests/helpers.cjs: `runGsdTools`, `createTempProject`, and `cleanup` contracts; its `parseFrontmatter` helper is scalar-only and must not be used for nested `progress` assertions.
    - tests/frontmatter.unit.test.cjs: the repository convention for `const yaml = require('js-yaml')` and strict YAML parsing.
  </read_first>
  <behavior>
    - RED: With `TZ=UTC`, `GSD_TEST_MODE=1`, and `GSD_NOW_MS` pinned to midday `2020-09-10T12:00:00.000Z`, a temporary STATE.md has authoritative frontmatter activity values and body prose carrying the same date with a different description.
    - GREEN: The pinned `state planned-phase --phase 1 --plans 3` command retains both authoritative activity values in final raw frontmatter for that conflict.
    - Boundary: A second pinned fixture whose body activity is eligible for the planned-phase update and whose pre-transition frontmatter date does not match the transition date ends with the command's new date and planning-complete description, not the old frontmatter values.
    - Regression oracle: Each test reads final STATE.md, isolates the YAML block, and loads it with `js-yaml`; neither uses `state json` nor the scalar-only test helper.
  </behavior>
  <action>First add focused command-level RED coverage in `tests/state.test.cjs`, following the existing temporary-project lifecycle. Require the already-installed dev dependency with `const yaml = require('js-yaml')`; create a local raw-frontmatter helper that extracts the leading YAML block and calls `yaml.load`, because `tests/helpers.cjs` `parseFrontmatter` intentionally ignores nested values. In both command invocations pass `{ GSD_TEST_MODE: '1', GSD_NOW_MS: String(Date.parse('2020-09-10T12:00:00.000Z')), TZ: 'UTC' }`, so the documented `realClock.localToday()` value is deterministically `2020-09-10`. The first fixture must give frontmatter non-empty `last_activity: 2020-09-10` and `last_activity_desc: authoritative description`, while the body has `Last Activity: 2020-09-10 — stale description` and no separate body description; assert final raw YAML retains both authoritative values. Add the boundary fixture in the same suite with a nonmatching pre-transition frontmatter activity date and a body value that `plannedPhaseCore` recognizes as a template default; assert the final raw YAML contains `2020-09-10` and `Phase 1 planning complete — 3 plans ready`. Implement the repair in `cmdStatePlannedPhase` with a `ReadModifyWriteOptions` object held across the locked callback. Before `transitionCore`, inspect the lock-held pre-transition frontmatter and stripped body using the existing `stateExtractField` and `parseProseLastActivityField` precedence: set `authoritativeFm` for exactly `last_activity` and `last_activity_desc` only when both frontmatter values are non-empty strings, the body-derived activity date equals the frontmatter date, and the body-derived description is non-empty and differs from the frontmatter description. Pass the options object to `readModifyWriteStateMd`. Do not alter `plannedPhaseCore`, the global synchronization policy, the field-classification table, or unrelated transitions.</action>
  <verify>
    <automated>node scripts/run-tests.cjs --files tests/state.test.cjs</automated>
  </verify>
  <acceptance_criteria>The conflict test fails before the repair and passes after it with `last_activity` and `last_activity_desc` exactly retained; the non-conflicting boundary test proves the serialized activity date and description are written by the planned-phase transition.</acceptance_criteria>
  <done>The planned-phase path supplies the two activity values through the existing lock-safe option only for the defined same-date description conflict, and both focused public-command regressions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove planned-phase's intended transition effects survive the preservation repair</name>
  <files>tests/state.test.cjs</files>
  <read_first>
    - tests/state.test.cjs: the Task 1 regressions and nearby planned-phase tests that assert status, Current Position, and `progress.total_plans` behavior.
    - tests/frontmatter.unit.test.cjs: the existing strict `js-yaml` parsing pattern required to inspect the nested serialized progress object.
    - src/state-transition.cts: `plannedPhaseCore`'s status, Current Position, explicit plan-count, and frontmatter progress behavior.
    - src/state.cts: the post-sync preservation call inside `readModifyWriteStateMd`, to keep test expectations aligned with the final serialized artifact.
  </read_first>
  <behavior>
    - The same regression command retains the planned-phase status transition in raw frontmatter and the intended labeled body/current-position update.
    - The explicit `--plans 3` input remains reflected in the planned-phase plan-count body field and in `progress.total_plans` parsed from the nested final YAML object.
    - The two retained activity values are not treated as permission to skip the planned-phase transition.
  </behavior>
  <action>Expand the same-date conflict regression from Task 1 so its final-artifact assertions prove the complete contract: retained authoritative activity date and description, planned-phase status, current-position behavior, and explicit plan count in the `Total Plans in Phase` body field. Reuse Task 1's strict raw-YAML helper for the nested assertion and require `parsed.progress.total_plans === 3`; do not use the flat `parseFrontmatter` helper for this object. Keep body checks bounded to the relevant labeled fields/current-position section and assert runtime behavior rather than source text. Keep the scope to the confirmed conflict plus the Task 1 non-conflicting boundary, without creating a broad synchronization matrix.</action>
  <verify>
    <automated>node scripts/run-tests.cjs --files tests/state.test.cjs</automated>
  </verify>
  <acceptance_criteria>The focused test confirms the final STATE.md simultaneously retains the two authoritative activity fields and reflects the expected planned-phase status, body/current-position, `Total Plans in Phase`, and `progress.total_plans` changes for three plans.</acceptance_criteria>
  <done>QUALITY-01 is enforced by a single focused public-command regression that reads the final artifact and covers preservation plus normal planned-phase effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state planned-phase CLI input → locked STATE.md mutation | A caller-provided phase and plan count trigger a transition over user-authored state content. |
| STATE.md body prose → regenerated frontmatter | Potentially stale prose is parsed during synchronization and can conflict with authoritative frontmatter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `cmdStatePlannedPhase` / `readModifyWriteStateMd` | medium | mitigate | Preserve the lock-held activity pair through `authoritativeFm` only for the same-date, differing-description body conflict; prove a non-conflicting update still derives the transition activity. |
| T-01-02 | Tampering | `tests/state.test.cjs` regression oracle | low | mitigate | Parse raw final frontmatter with `js-yaml` after invoking the public command, then assert the nested `progress.total_plans` object and planned-phase body effects. |
</threat_model>

<verification>
Run `node scripts/run-tests.cjs --files tests/state.test.cjs`. The command builds the current TypeScript artifacts through the repository test runner, then executes the focused state command suite. Review the new regression's raw-final-STATE.md assertions for both activity preservation and normal planned-phase changes.
</verification>

<success_criteria>
1. `state planned-phase` cannot overwrite authoritative `last_activity` or `last_activity_desc` when body prose has the same date but a different description.
2. A non-conflicting planned-phase update retains the normal transition-derived activity date and description, while the conflict fixture preserves status, body/current-position, and explicit plan-count effects.
3. The repository has deterministic, focused public-command regressions that parse the final nested YAML artifact and fail against the pre-repair behavior.
</success_criteria>

## Artifacts this phase produces

- `src/state.cts` — planned-phase wiring that preserves authoritative activity metadata at the existing locked write seam.
- `tests/state.test.cjs` — final-artifact command regression for STATE-01 and QUALITY-01.

## Source Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Preserve authoritative activity metadata while planning a phase | 01-01 | COVERED | Task 1 routes only the defined same-date description conflict through the existing write seam. |
| REQ | STATE-01 | Same-date stale body prose cannot overwrite frontmatter activity values | 01-01 | COVERED | Task 1 creates the deterministic conflict fixture and a non-conflicting boundary. |
| REQ | QUALITY-01 | Focused command-level final-artifact regression also proves normal transition effects | 01-01 | COVERED | Task 2 expands the conflict scenario with strict nested progress parsing. |
| RESEARCH | — | No phase RESEARCH.md exists | 01-01 | COVERED | Focused source/test inspection substitutes for absent phase research; no unverified external dependency is planned. |
| CONTEXT | — | No CONTEXT.md decisions exist | 01-01 | COVERED | No locked decisions to implement. |

<output>
Create `.planning/phases/01-planned-phase-state-integrity/01-01-SUMMARY.md` when done.
</output>
