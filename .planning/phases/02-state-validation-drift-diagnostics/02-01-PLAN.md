---
phase: 02-state-validation-drift-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state.cts
  - tests/state.test.cjs
autonomous: true
requirements:
  - STATE-02
  - QUALITY-02

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Per D-01, state.validate selects an active phase from frontmatter current_phase, then legacy body Current Phase, then the canonical Current Position Phase field."
    - "Per D-02, canonically different phase sources remain observable as phase-reference drift while the authoritative winner is still scanned."
    - "Per D-03, a state document with no usable phase source returns valid: false with an actionable warning and structured phase-reference drift."
    - "Per D-04, a resolved phase whose phases root or canonical directory match is absent returns valid: false with missing-phase drift."
    - "Per D-05 and D-06, a template-derived command regression reaches a known on-disk drift condition, and an equivalent clean fixture proves the measurement can also return no drift."
    - "Canonical phase-key equality treats equivalent spellings such as 2 and 02 as the same phase and never constructs a scan path from raw state text."
  artifacts:
    - path: "src/state.cts"
      provides: "Frontmatter-first active-phase resolution and warning-backed disk-drift diagnostics in cmdStateValidate"
      contains: "function cmdStateValidate"
    - path: "tests/state.test.cjs"
      provides: "Template-derived end-to-end regression and precedence/failure/normalization controls in the owning command suite"
      contains: "describe('state validate command'"
  key_links:
    - from: "src/state.cts::cmdStateValidate"
      to: "src/frontmatter.cts::extractFrontmatter and src/state-document.cts::stateExtractField"
      via: "separate frontmatter, legacy-body, and Current Position prose source extraction before D-01 selection"
      pattern: "extractFrontmatter|stripFrontmatter|matchCurrentPositionSection|stateExtractField|parseProsePhaseField"
    - from: "src/state.cts::cmdStateValidate"
      to: "src/phase-id.cts::phaseKeyFromToken and phaseKeyFromDir"
      via: "canonical source comparison and equality against enumerated directory entries"
      pattern: "phaseKeyFrom(Token|Dir)"
    - from: "src/state.cts::cmdStateValidate"
      to: "src/plan-scan.cts::scanPhasePlans"
      via: "the trusted enumerated phase directory selected by canonical key"
      pattern: "scanPhasePlans"
    - from: "tests/state.test.cjs"
      to: "gsd-core/templates/state.md and the emitted gsd-tools CLI"
      via: "template-derived STATE.md fixtures executed through runGsdTools('state validate') after build:lib"
      pattern: "runGsdTools\\('state validate'"
  assumptions:
    - statement: "Spec-less edge probe classified STATE-02 as unresolved; locked decisions D-01 through D-04 and the command-level source matrix below are the explicit acceptance surface."
      status: unresolved
      flagged: true
    - statement: "Spec-less edge probe classified QUALITY-02 as unresolved; D-05 and D-06 plus the positive-drift and clean-control pair below are the explicit acceptance surface."
      status: unresolved
      flagged: true
  prohibitions: []
---

<objective>
Repair the existing `state.validate` path so a shipped-shape `STATE.md` resolves its active phase and reaches real on-disk drift checks, with source precedence and failure behavior fixed by D-01 through D-04 and command-level proof fixed by D-05 and D-06.

Purpose: Make a clean validation result mean that the validator actually located and scanned the intended phase, rather than silently skipping its subject.
Output: A focused `cmdStateValidate` repair and an owning-suite regression matrix with a template-derived positive drift case and opposite-result controls.
</objective>

<execution_context>
@/home/denniyahh/.codex/gsd-core/workflows/execute-plan.md
@/home/denniyahh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-state-validation-drift-diagnostics/02-CONTEXT.md
@.planning/phases/02-state-validation-drift-diagnostics/02-RESEARCH.md
@.planning/phases/02-state-validation-drift-diagnostics/02-PATTERNS.md
@.planning/phases/02-state-validation-drift-diagnostics/02-VALIDATION.md
@scratch/UPSTREAM-GSD-ISSUES.md
@src/state.cts
@src/phase-id.cts
@src/frontmatter.cts
@src/state-document.cts
@src/plan-scan.cts
@gsd-core/templates/state.md
@tests/state.test.cjs
@tests/helpers.cjs

<interfaces>
Existing interfaces the executor must preserve and reuse:

- `src/state.cts`: `cmdStateValidate(cwd: string, raw: boolean): void`, `matchCurrentPositionSection(body: string): string | null`, and `parseProsePhaseField(value: string | null): { phase: string | null; name: string | null }`.
- `src/frontmatter.cts`: `extractFrontmatter(content, sourcePath)` returns a mapping and `stripFrontmatter(content)` returns the body.
- `src/state-document.cts`: `stateExtractField(content: string, fieldName: string): string | null` supports bold, plain, and pipe-table body fields.
- `src/phase-id.cts`: `phaseKeyFromToken(token: unknown): string` and `phaseKeyFromDir(dirName: string): string` provide the shared padding-, case-, and project-code-insensitive identity.
- `src/plan-scan.cts`: `scanPhasePlans(phaseDir)` remains the sole plan/summary scanner.
- `tests/helpers.cjs`: `runGsdTools(args, cwd)` invokes the emitted CLI and `cleanup(tmpDir)` owns portable fixture cleanup.
</interfaces>
</context>

## Artifacts this phase produces

- Modified existing production symbol: `src/state.cts::cmdStateValidate` resolves three independent phase sources, reports source/directory failure, and scans only a canonical match selected from enumerated children.
- New structured drift members on the existing `{ valid, warnings, drift }` result: `drift.phase_reference` for unresolved/conflicting sources and `drift.phase_directory` for missing/unreadable phase targets; `drift.verification_status` and `drift.plan_count` remain unchanged.
- New test-only symbol in `tests/state.test.cjs`: `readShippedStateTemplateBody`, which extracts the fenced product fixture and fails loudly when the template shape or substitutions are unavailable.
- New command-regression cases in the existing `state validate command` suite for the template/frontmatter tracer, clean canonical-key equality, legacy and canonical fallbacks, source conflict, no source, missing phase directory/root, and crafted path-like input.
- No new CLI command, flag, production export, module, package, or generated artifact is introduced.

## Spec-less fallback record

- No phase SPEC or UI-SPEC exists. The default-on fallback ran against STATE-02 and QUALITY-02; both edge rows were `unclassified`/`unresolved`, so both appear as flagged `must_haves.assumptions` above and neither is silently promoted to covered or backstop.
- Adversarial prohibition recall produced no bespoke values, fairness, privacy, transparency, or safety prohibition for this local diagnostic utility after routine correctness items were dropped. Path traversal/path confusion is canon security work and is handled by T-02-01 plus the crafted-reference regression, not minted as a fallback prohibition.
- The explicit source artifacts already forbid a new verification command, progress-calculation changes, broad state synchronization, and DevFlow. Those fences are enforced in task actions and acceptance criteria; they are not inferred fallback prohibitions.
- No-silent-drop accounting: 2 edge items = 2 flagged assumptions + 0 covered/backstop truths; 0 kept prohibition items = 0 `must_haves.prohibitions` entries.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and repair the shipped-template frontmatter path end to end</name>
  <reversibility rating="costly">The additive `phase_reference` and `phase_directory` drift member names become observable CLI result fields; changing them later could require coordinated consumer and regression updates, while D-01 through D-04 themselves are already locked.</reversibility>
  <files>src/state.cts, tests/state.test.cjs</files>
  <read_first>
    - .planning/phases/02-state-validation-drift-diagnostics/02-CONTEXT.md
    - .planning/phases/02-state-validation-drift-diagnostics/02-RESEARCH.md
    - .planning/phases/02-state-validation-drift-diagnostics/02-PATTERNS.md
    - .planning/phases/02-state-validation-drift-diagnostics/02-VALIDATION.md
    - scratch/UPSTREAM-GSD-ISSUES.md
    - src/state.cts
    - src/frontmatter.cts
    - src/state-document.cts
    - src/phase-id.cts
    - src/plan-scan.cts
    - gsd-core/templates/state.md
    - tests/state.test.cjs
    - tests/helpers.cjs
  </read_first>
  <behavior>
    - RED: A `STATE.md` derived from the fenced `gsd-core/templates/state.md` document, carrying frontmatter `current_phase: 2` and executing status, a matching enumerated phase directory, and a passed verification artifact must report `valid: false` plus existing `verification_status` drift; before the repair it returns clean because the disk gate never opens.
    - GREEN: The same command reaches `scanPhasePlans` and verification-file inspection through the frontmatter source without adding a legacy body field.
    - NEGATIVE CONTROL: A template-derived document with equivalent frontmatter/body phase identities, a matching empty phase directory, non-executing status, and no drift artifact must return `valid: true`, `warnings: []`, and `drift: {}`.
  </behavior>
  <action>
    In `tests/state.test.cjs`, first add `readShippedStateTemplateBody` beside `writePassedVerification`. Resolve `gsd-core/templates/state.md` through `path.join(__dirname, '..', ...)`, extract its fenced `markdown` document with CRLF-tolerant matching, and assert that the fence plus each placeholder/replacement target exists before constructing the fixture. Add the RED command regression inside the existing `describe('state validate command')`: inject frontmatter `current_phase: 2`, set frontmatter and Current Position status to an executing value, set canonical `Phase:` to `2 of 2 (State Validation Drift Diagnostics)`, create `.planning/phases/02-state-validation-drift-diagnostics/02-VERIFICATION.md` with `status: passed`, invoke `runGsdTools('state validate', tmpDir)`, and assert parsed JSON semantics rather than a full warning sentence. Add the opposite-result clean fixture described in `<behavior>` so the measurement cannot pass when drift detection is stuck permanently on or permanently off. This is the D-05 shipped-shape proof and the D-06 full-command requirement.

    After recording the failing tracer, modify only `cmdStateValidate` in `src/state.cts`. Split content into frontmatter and body with `extractFrontmatter(content, statePath)` and `stripFrontmatter(content)`; accept only non-empty string/number/boolean `current_phase` scalars, retain raw source provenance, and derive usable phase tokens through the existing anchored `parseProsePhaseField` seam. Resolve independent `current_phase`, legacy body `Current Phase`, and Current Position-scoped canonical body `Phase:` sources in the exact D-01 order. Compare usable sources with `phaseKeyFromToken`; if a non-authoritative source has a different canonical key, keep scanning the authoritative winner but add an actionable warning and `drift.phase_reference` containing the selected token, source tokens, and reason `conflict` per D-02. If no source yields a usable token, add an actionable warning and `drift.phase_reference` with selected `null` and reason `unresolved`, then return the existing `{ valid, warnings, drift }` contract with `valid: false` per D-03.

    For a resolved token, enumerate directory entries under `planningPaths(cwd).phases`, restrict candidates to directories, compare `phaseKeyFromDir(entry.name)` to `phaseKeyFromToken(resolvedToken)`, and join only the trusted enumerated `entry.name`; do not derive a path from the raw state value. Keep `scanPhasePlans`, existing plan-count/status checks, the NUL/binary early return, and per-verification-file best-effort behavior intact. Record an actionable warning plus `drift.phase_directory` for an absent phases root, no canonical match, or a phase-root scan failure so an unperformed required scan cannot fall through as valid per D-04. Do not add `state.verify-against-disk`, alter progress/sync logic, modify the shipped template, edit emitted `gsd-core/bin/lib/*.cjs`, or add dependencies.
  </action>
  <acceptance_criteria>
    - `tests/state.test.cjs` contains `readShippedStateTemplateBody` and fails loudly if the fenced template document or concrete replacement targets are missing.
    - The template/frontmatter positive case invokes `runGsdTools('state validate', tmpDir)` and asserts `valid === false`, a non-empty warning set, and `drift.verification_status` from a real on-disk `status: passed` artifact.
    - The matched clean control invokes the same command and asserts `valid === true`, `warnings` is empty, and `drift` is empty.
    - `cmdStateValidate` uses D-01 precedence, Current Position scoping, canonical phase keys, enumerated directory names, and the existing `scanPhasePlans`; the positive tracer does not depend on a body `Current Phase` field.
    - Unresolved sources and missing/unreadable phase targets populate warning-backed `phase_reference` or `phase_directory` drift instead of returning clean validation.
    - The executor records the pre-fix RED result and post-fix GREEN result in the task summary; a passing legacy baseline alone is not reported as proof of STATE-02.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:lib &amp;&amp; node --test --test-name-pattern='state validate command' tests/state.test.cjs</automated>
  </verify>
  <done>The shipped-template/frontmatter fixture reaches real verification drift through the emitted CLI, the clean control produces the opposite result, and missing validation subjects fail closed without a new command or scanner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand precedence, compatibility, failure, normalization, and path-confusion coverage</name>
  <files>src/state.cts, tests/state.test.cjs</files>
  <read_first>
    - .planning/phases/02-state-validation-drift-diagnostics/02-CONTEXT.md
    - .planning/phases/02-state-validation-drift-diagnostics/02-RESEARCH.md
    - .planning/phases/02-state-validation-drift-diagnostics/02-VALIDATION.md
    - src/state.cts
    - src/phase-id.cts
    - src/frontmatter.cts
    - src/state-document.cts
    - src/plan-scan.cts
    - gsd-core/templates/state.md
    - tests/state.test.cjs
    - tests/helpers.cjs
  </read_first>
  <behavior>
    - D-01 COMPATIBILITY: Legacy body `Current Phase` alone and Current Position canonical `Phase:` alone each select the matching phase directory and reach the same known verification drift path.
    - D-02 CONFLICT: Frontmatter phase 2 plus canonical body phase 1 reports `phase_reference` conflict, scans phase 2, and exposes phase 2 disk evidence rather than silently selecting phase 1.
    - D-03 NEGATIVE: No phase source, or a non-scalar/malformed frontmatter source with no body fallback, returns `valid: false` and unresolved `phase_reference` drift.
    - D-04 NEGATIVE: A usable phase with no phases root or no canonical directory match returns `valid: false` and `phase_directory` drift.
    - NORMALIZATION CONTROL: Frontmatter `2`, canonical body `02`, and directory `02-state-validation-drift-diagnostics` agree canonically and produce no phase-reference or phase-directory warning; in a drift-free fixture the whole result stays clean.
    - PATH-CONFUSION CONTROL: A path-like crafted phase value is unusable or unmatched, never scanned outside `.planning/phases`, and cannot surface verification evidence planted outside the enumerated phase root.
  </behavior>
  <action>
    Extend the existing `state validate command` suite with separate command-level cases for every D-06 source branch and every failure/negative-space row in `<behavior>`. Reuse `writePassedVerification`, `readShippedStateTemplateBody`, `createFixture`, `runGsdTools`, and suite cleanup; build multiline fixture content with line arrays joined by newline where a template-derived body is not required. For the conflict case, create both phase 1 and phase 2 directories but place the known passed-verification evidence only under phase 2; assert the selected phase evidence and `drift.phase_reference.reason === 'conflict'`, which proves frontmatter authority and observable disagreement simultaneously. For legacy-only and canonical-only cases, remove the higher-precedence sources instead of leaving an equivalent frontmatter value that would mask the branch under test.

    Add exact opposite-result controls: the `2`/`02`/`02-*` equality fixture must remain entirely clean, while no-source, non-scalar-source, missing-root, missing-match, and crafted path-like fixtures must be invalid with the relevant structured drift member and an actionable warning category. For the crafted reference case, plant a passed verification artifact outside `.planning/phases` and assert that `verification_status` is absent, demonstrating that only enumerated child names can feed `path.join`. Preserve the current legacy tests and nested-layout tests so `scanPhasePlans` plan/summary semantics do not regress. Adjust only the local `cmdStateValidate` resolution/diagnostic block if these tests reveal an incomplete branch; do not add new parsers, scanners, commands, package changes, progress calculations, synchronization behavior, or generated-file edits.
  </action>
  <acceptance_criteria>
    - Frontmatter, legacy `Current Phase`, and Current Position `Phase:` are exercised in separate emitted-CLI cases, and legacy/canonical fallback cases each reach a real disk-backed `verification_status` finding.
    - A frontmatter/canonical conflict reports `drift.phase_reference.reason === 'conflict'`, retains the frontmatter-selected phase token, and reports disk evidence from that phase.
    - No usable source reports `drift.phase_reference.reason === 'unresolved'`; missing root/match reports `drift.phase_directory`; all such results have `valid === false` and actionable warnings.
    - Equivalent `2` and `02` sources plus the `02-*` directory return a fully clean result when no disk drift is present, proving canonical normalization does not create false positives.
    - The crafted path-like source cannot trigger `verification_status` from outside `.planning/phases`, and the production path join consumes only an enumerated directory entry name.
    - Existing plan-count, nested-plan, NUL/binary, missing-STATE, and graceful verification-file scan behaviors remain green in the owning suite.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:lib &amp;&amp; node --test --test-name-pattern='state validate command' tests/state.test.cjs</automated>
  </verify>
  <done>The focused suite proves all locked source paths, disagreement, fail-closed boundaries, canonical equality, and safe directory selection through `state validate`, including positive and opposite-result controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `STATE.md` → `cmdStateValidate` | Project-controlled text and YAML-shaped frontmatter cross into phase selection and diagnostic policy. |
| `cmdStateValidate` → `.planning/phases/*` | A parsed phase identity selects which filesystem subtree is scanned; raw document text must not become a path. |
| Phase artifacts → diagnostic JSON | Plan, summary, and verification files influence warnings and structured drift returned to callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `cmdStateValidate` phase text → directory selection | high | mitigate | Parse only usable phase tokens, compare with `phaseKeyFromToken`/`phaseKeyFromDir`, enumerate directory children, and join only the matched `entry.name`; Task 2 plants outside-root evidence as a negative control. |
| T-02-02 | Spoofing | Missing source/root/match reported as a clean validation | high | mitigate | D-03/D-04 require warning-backed `valid:false` with `phase_reference` or `phase_directory` drift, covered by distinct command regressions. |
| T-02-03 | Denial of Service | Crafted or very long `Phase:` prose | low | accept | The local CLI reuses the existing anchored and length-bounded `parsePhaseFromProse` seam; no new regex or recursive scan is introduced, so residual local-project input risk is tolerable at ASVS L1. |
| T-02-04 | Tampering | Binary/NUL state content collapsing into absent fields | medium | mitigate | Preserve the existing `textEncodingError` early invalid result before any source resolution, and retain its owning-suite coverage. |
| T-02-SC | Tampering | npm/pip/cargo install supply chain | high | mitigate | This phase permits no package install or manifest/lockfile change and uses only existing repository dependencies; an executor must stop if an install becomes necessary because no package-legitimacy audit is in scope. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Shipped-shape state validation resolves the active phase and reports corresponding disk drift | 02-01 | COVERED | Tracer proves the complete CLI path; expansion covers failure and compatibility. |
| REQ | STATE-02 | Frontmatter-first resolution with canonical prose fallback reaches active-phase drift on disk | 02-01 | COVERED | Tasks 1-2 implement D-01 through D-04 and safe canonical directory matching. |
| REQ | QUALITY-02 | Focused regression proves drift for the normal state-document shape | 02-01 | COVERED | Task 1 derives the main fixture from the shipped template and pairs positive/clean controls. |
| RESEARCH | — | Parse sources independently before precedence and conflict comparison | 02-01 | COVERED | Task 1 retains source provenance and selects by D-01. |
| RESEARCH | — | Compare canonical phase identities and join only an enumerated directory name | 02-01 | COVERED | Tasks 1-2 plus T-02-01 specify equality and path-confusion proof. |
| RESEARCH | — | Fail closed when source, phases root, directory match, or required scan is unavailable | 02-01 | COVERED | Tasks 1-2 require warning-backed structured drift. |
| RESEARCH | — | Preserve `scanPhasePlans`, encoding rejection, and existing diagnostic behavior | 02-01 | COVERED | Both actions prohibit replacement and acceptance retains existing tests. |
| RESEARCH | — | Use full emitted-CLI regressions, template-derived positive drift, and negative controls | 02-01 | COVERED | Task 1 tracer plus Task 2 source/failure matrix. |
| CONTEXT | D-01 | Frontmatter → legacy Current Phase → canonical Phase precedence | 02-01 | COVERED | Tasks 1-2 cite and exercise each branch. |
| CONTEXT | D-02 | Frontmatter authoritative; disagreement observable | 02-01 | COVERED | Conflict retains winner and records `phase_reference` drift. |
| CONTEXT | D-03 | Unresolvable phase is invalid with actionable warning | 02-01 | COVERED | No-source and malformed-source command cases. |
| CONTEXT | D-04 | Missing phase directory is invalid drift | 02-01 | COVERED | Missing root and missing canonical match cases. |
| CONTEXT | D-05 | Main regression derives from shipped template and introduces disk drift | 02-01 | COVERED | Task 1 tracer. |
| CONTEXT | D-06 | Separate frontmatter, legacy, canonical, and no-source command coverage | 02-01 | COVERED | Task 2 matrix, with parser-only proof explicitly disallowed. |

Deferred STATE-03, PROGRESS-01, broad state synchronization, and DevFlow are exclusions rather than audit gaps and do not appear as implementation work.

<verification>
- After each task commit: `npm run build:lib && node --test --test-name-pattern='state validate command' tests/state.test.cjs`.
- After Wave 1: `node scripts/run-tests.cjs --files state.test.cjs` and `npm run lint`.
- Before phase verification: `npm test` and `npm run lint:ci`.
- Negative-control interpretation: the positive template fixture proves the disk path can report drift; the clean equality fixture proves the measurement is not permanently failing; absent/crafted fixtures prove the validator fails closed rather than silently passing.
- Passing the focused command suite establishes only the exercised state-validation behaviors. Cross-platform repository compatibility and unrelated command behavior require the full test and lint gates above.
</verification>

<success_criteria>
- A normal template-derived `STATE.md` with frontmatter `current_phase` selects its canonical phase directory and reports known disk drift through the existing CLI result.
- Legacy `Current Phase` and canonical Current Position `Phase:` remain functional fallbacks in D-01 order.
- Canonically different sources report phase-reference drift while the authoritative phase is scanned; equivalent formatting remains clean.
- Missing/unusable sources and missing/unreadable phase targets are invalid, warning-backed results.
- Focused, owning-suite, full-test, lint, and lint-contract commands complete successfully, with their evidence limits recorded in the summary.
</success_criteria>

<output>
Create `.planning/phases/02-state-validation-drift-diagnostics/02-01-SUMMARY.md` when done.
</output>
