---
phase: quick-260922-me4
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
quick_id: 260922-me4
description: "fix(#4686): phase uat-passed and verify.artifacts exit non-zero on negative verdict"
files_modified:
  - src/verify.cts
  - src/phase.cts
  - tests/verify.test.cjs
  - tests/phase.test.cjs
must_haves:
  truths:
    - "query verify.artifacts on a plan with a failing must_haves.artifacts check exits non-zero (exit code 1) under both v1 and v2 exit contracts while printing all_passed: false in JSON."
    - "phase uat-passed on a phase with failing UAT or failing/stale/human_needed verification under --require-verification exits non-zero (exit code 1) under both v1 and v2 exit contracts while printing passed: false in JSON."
    - "Both verbs continue to exit 0 on an affirmative verdict (all_passed: true / passed: true)."
    - "Both verbs keep their existing precondition-error exit codes and payloads untouched."
  artifacts:
    - path: "src/verify.cts"
      provides: "Exit code 1 and FAIL outcome declaration when cmdVerifyArtifacts computes all_passed: false"
      contains: "process.exitCode = 1"
    - path: "src/phase.cts"
      provides: "Exit code 1 and FAIL outcome declaration when cmdPhaseUatPassed computes report.passed: false"
      contains: "process.exitCode = 1"
    - path: "tests/verify.test.cjs"
      provides: "Regression tests asserting exit code 1 on negative artifact verification"
      contains: "4686"
    - path: "tests/phase.test.cjs"
      provides: "Regression tests asserting exit code 1 on negative UAT and verification predicates"
      contains: "4686"
  key_links:
    - from: "src/verify.cts"
      to: "src/cli-exit.cts"
      relationship: "Declares outcome FAIL to exit contract seam and sets process.exitCode on failure"
    - from: "src/phase.cts"
      to: "src/cli-exit.cts"
      relationship: "Declares outcome FAIL to exit contract seam and sets process.exitCode on failure"
---

# Quick Task 260922-me4: fix(#4686) CLI gate verbs exit non-zero on failure

<tasks>
<task>
<name>Declare failure outcomes in cmdVerifyArtifacts and cmdPhaseUatPassed</name>
<files>
src/verify.cts
src/phase.cts
</files>
<action>
1. In `src/verify.cts` within `cmdVerifyArtifacts`:
   - Import `setPendingOutcome` from `./cli-exit.cjs`.
   - After `output(...)` emits the result payload, if `!allPassed`, set `setPendingOutcome('FAIL')` and `process.exitCode = 1`.
2. In `src/phase.cts` within `cmdPhaseUatPassed`:
   - Import `setPendingOutcome` from `./cli-exit.cjs`.
   - After `output(...)` emits the result payload, if `!report.passed`, set `setPendingOutcome('FAIL')` and `process.exitCode = 1`.
3. Keep all precondition error arms (`!content`, `!artifacts.length`, `!phaseNum`, missing phase directory) completely untouched.
</action>
<verify>
Compile with `npm run build:lib` and inspect emitted files.
</verify>
<done>
Both `cmdVerifyArtifacts` and `cmdPhaseUatPassed` set `process.exitCode = 1` and declare `FAIL` when the computed verdict is negative.
</done>
</task>

<task>
<name>Update existing negative-verdict tests and add regression tests for #4686</name>
<files>
tests/verify.test.cjs
tests/phase.test.cjs
</files>
<action>
1. In `tests/verify.test.cjs`:
   - Update tests in `describe('verify artifacts command')` that expected `result.success` on failing artifact checks to expect `result.exitCode === 1` and `result.success === false`.
   - Add explicit regression test block for #4686 covering:
     - `verify artifacts` and `query verify.artifacts` with failing artifacts exits 1 under default contract (v1).
     - `verify artifacts` and `query verify.artifacts` with failing artifacts exits 1 under `--exit-contract=v2`.
     - Affirmative artifact check exits 0.
2. In `tests/phase.test.cjs`:
   - Update tests in `describe('phase uat-passed — basic pass/fail')` and `describe('phase uat-passed — --require-verification flag')` that expected `result.success` on `passed: false` to expect `result.exitCode === 1`.
   - Add explicit regression test block for #4686 covering:
     - `phase uat-passed` on pending/failing UAT exits 1 under default contract (v1) and `--exit-contract=v2`.
     - `phase uat-passed --require-verification` on stale or missing verification exits 1 under default contract (v1) and `--exit-contract=v2`.
     - Affirmative UAT + verification exits 0.
</action>
<verify>
Run focused tests on remote Mac runner: `mise run test:mac tests/verify.test.cjs tests/phase.test.cjs`.
</verify>
<done>
All tests in `tests/verify.test.cjs` and `tests/phase.test.cjs` pass cleanly on Mac runner.
</done>
</task>

<task>
<name>Changeset and verification gate</name>
<files>
.changeset/*.md
</files>
<action>
1. Generate changeset for patch fix with PR reference #4686 and docs-exempt marker (internal CLI bug fix).
2. Run `mise run check:budget` to verify workflow byte budgets.
3. Validate working tree status.
</action>
<verify>
`mise run test:mac tests/verify.test.cjs tests/phase.test.cjs` and `mise run check:budget`.
</verify>
<done>
Changeset created and ready for verification gate.
</done>
</task>
</tasks>
